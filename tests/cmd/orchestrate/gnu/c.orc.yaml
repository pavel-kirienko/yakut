$=:
  - echo $FOO
  - ?=:  # Since this is an optional block, its failure will be ignored.
      call=: nonexistent.orc.yaml
      .=: fgsfds
  -
  - $=: echo $FOO
    FOO: 456
  -
  - call=: tests/cmd/orchestrate/gnu/b.orc.yaml  # Recursive inclusion but with the gate flag cleared.
    PROCEED_B:
.=:
  ?=: test -n "$PLEASE_FAIL"
  $=: exit 42

PASSWORD: hunter2
BAR: 789
