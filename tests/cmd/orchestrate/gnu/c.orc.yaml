main=:
  - echo $FOO
  - ?=:  # Since this is an optional block, its failure will be ignored.
      external=: nonexistent.orc.yaml
      finally=: fgsfds
  -
  - main=: echo $FOO
    FOO: 456
  -
  - external=: tests/cmd/orchestrate/gnu/b.orc.yaml  # Recursive inclusion but with the gate flag cleared.
    PROCEED_B:
finally=:
  ?=: test -n "$PLEASE_FAIL"
  main=: exit 42

PASSWORD: hunter2
BAR: 789
